# D5 日报

## 昨日遗留问题的回答

（今日尚未收到系统对 D4 的反馈，收到后于下节补充作答。）

## 今日目标

1. 学生端历史记录去掉遮挡日期的状态圆点（绿点）
2. 学生端请假卡片写明"几点到几点"完整时段
3. 完成当日 daily/prompts/飞书记录同步（按本地日 9/5 分账）

## 实际进展

- 恢复本地服务：MySQL / 后端（V1.9 重构版 jar）/ 前端（V1.7 主题 + V1.8 模块化）全部就绪
- `5cdec68`（9/5 14:18）：学生端历史记录移除每条的 `.dot` 状态圆点，日期不再被遮挡；状态仍以右侧标签显示；已双源推送
- `e3dbb6a`（9/5 15:03）：① 学生端对已下课课程不可请假（请假列表只显示未下课已确认课时 + 后端 submitLeave 新增 ENDED 校验）；② 老师端待完成提醒——下课超 20 分钟仍未登记完成的课时，工作台顶部横幅「这节课已过去，但老师未点击完成」可一键登记完成；③ 请假卡片显示完整时段 M/D HH:mm-HH:mm；集成测试 12/12 通过，冒烟确认王老师当前 2 节课进入提醒
- `f19e412`（9/5 16:xx）：管理员停课 —— 新增 TimeslotBlock 实体/Mapper + BlockService，复用预留表 timeslot_block：管理员可为某老师某日停课（全天或按时段屏蔽）；生效三处（学生端可约 slotsOn、老师端周课表未来日、管理员直排 plan/book 均拦）；GET/POST/DELETE /api/admin/blocks 仅管理员；前端排课 tab 增「停课」卡（选老师+日期+全天勾选或起止+原因，列表可恢复）；另排课下拉只显示姓名、演示账号改 2-3 字真名；集成测试 3 用例共 15/15，冒烟增/查/重复409/删通过
- 学生端请假卡片由"仅开始时间"改为完整时段 `M/D HH:mm-HH:mm`（复用 `utils/datetime.mdHmRange`）；改动已完成，待用户确认后提交（LeaveView 改动暂存工作区，不擅自 push）
- 记录：D5 prompts 导出（截至 14:24 快照 3 条）+ 本篇日报

## 遇到的问题与解决

1. **请假只显示开始时刻**：原 `mdHm(startAt)` 只有 `HH:mm`，看不出下课时间。改用 `mdHmRange(startAt, endAt)`，与全站 datetime 工具口径统一，一处定义全站生效。
2. **分日记账错位**：一度把 9/5 发生的工作（绿点、请假）记到 D4(9/4) 日报。已还原 D4.md，改为 9/5 独立建 D5 记录，保证「当日事当日记」口径正确。

## 明日计划

1. 用户手测停课：停课后学生端约不到、老师端当天无可约、恢复后时段回归
2. 今日会话结束收尾补全 D5 prompts（当前已导 5 条）
3. 待系统对 D4/D5 反馈到达后补「昨日遗留问题的回答」
4. 继续 R10 商品售卖（组长分工由我编码）或 Docker Compose 部署
