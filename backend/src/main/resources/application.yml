server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/yueke?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: ""
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto

app:
  jwt:
    secret: bookmate-dev-secret-bookmate-dev-secret-bookmate-dev
    expire-hours: 24
